name: GitHub Actions Demo
on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
  schedule:
    - cron: "0 15 6 ? * SUN *"

jobs:
  my_job:
    name: build
    runs-on: ubuntu-18.04
